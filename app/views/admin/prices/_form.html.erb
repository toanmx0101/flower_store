<%= form_for(admin_price) do |f| %>
  <% if admin_price.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admin_price.errors.count, "error") %> prohibited this admin_price from being saved:</h2>

      <ul>
      <% admin_price.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :cost %>
    <%= f.text_field :cost %>
  </div>

  <div class="field">
    <%= f.label :discount %>
    <%= f.text_field :discount %>
  </div>

  <div class="field">
    <%= f.label :new %>
    <%= f.check_box :new %>
  </div>

  <div class="field">
    <%= f.label :product_detail_id %>
    <%= f.number_field :product_detail_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
