<%= form_for(admin_special) do |f| %>
  <% if admin_special.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admin_special.errors.count, "error") %> prohibited this admin_special from being saved:</h2>

      <ul>
      <% admin_special.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :product_id %>
    <%= f.number_field :product_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
